branches:
  only:
    - /^2.*$/
language: node_js
node_js:
  - 0.10
before_script:
  - npm install -g grunt-cli
notifications:
  email:
    recipients:
      - mark.carver@me.com
    on_success: never # default: change
    on_failure: always # default: always
  irc:
    irc: "irc.freenode.org#dreditor"
